<div class="row">
  <div class='col-md-9'>
    <div class="search-wide">
      <%= form_tag(users_path, :method => "get", :class => "form-horizontal", :role => "form") do %>
        <div class="input-group">
          <%= search_field_tag :query, params[:query], :placeholder => "Search user", :class => "form-control" %>
          <div class="input-group-btn">
            <button type="submit" class="btn btn-primary hidden-xs">Search</button>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>

<div class="row">
  <div class='col-md-9'>
    <% if @users.size > 0 %>
      <div class="panel-group" id="user">
        <% @users.each do |user| %>
          <div class="panel panel-default">
            <div class="panel-heading panel-title panel-list">
              <%= link_to ("#{h(user.name.to_s)}<small class='pull-right'>#{l user.created_at, format: :medium}</small>").html_safe, "#user_#{user.id}", :class => "accordion-toggle", :id => "link_#{user.id}", :data => { :toggle => "collapse", :parent => "#user" }, :href => "#user_#{user.id}" %>
            </div>
            <% if can?(:manage, user) && @user && @user.id == user.id && (controller.action_name == "edit" or (controller.action_name == "update" and !@user.errors.empty?)) %>
              <div id="user_<%= user.id %>" class="panel-collapse collapse.in">
                <div class="panel-body">
                  <%= simple_form_for user, url: user_path(user), html: { role: "form", remote: true } do |f| %>
                    <%= f.input :api_key, label: "API key", input_html: { class: "form-user" } %>
                    <%= f.input :role, collection: roles.map { |role| [role.humanize, role] }, include_blank: false, input_html: { class: "form-user" } %>
                    <%= f.association :member, label_method: :title, input_html: { class: "form-user" } %>

                    <div class="form-group">
                      <%= f.submit "Save ", class: "btn btn-default" %>
                      <%= link_to 'Cancel', users_path(query: params[:query], role: params[:role]), { :remote => true, :class => 'btn' } %>
                    </div>
                  <% end %>
                </div>
              </div>
            <% else %>
              <div id="user_<%= user.id %>" class="panel-collapse collapse">
                <div class="panel-body user-list">
                  <% if can?(:manage, user) && user.id != current_user.id %>
                    <div class="btn-toolbar pull-right">
                      <div class="btn-group btn-group-sm">
                        <%= link_to icon("pencil").html_safe, edit_user_path(user, query: params[:query], role: params[:role]), remote: true, class: 'btn btn-default btn-sm', id: "#{user.id}-edit" %>
                      </div>
                      <div class="btn-group btn-group-sm">
                        <%= link_to icon("trash").html_safe, user_path(user, query: params[:query], role: params[:role]), method: :delete, data: { confirm: 'Are you sure?' }, remote: true, class: 'btn btn-default btn-sm', id: "#{user.id}-delete" %>
                      </div>
                    </div>
                  <% end %>

                  <% if user.email.present? %>
                    <h5>Email</h5>
                    <%= link_to h(user.email), "mailto:#{user.email}" %>
                  <% elsif user.unconfirmed_email.present? %>
                    <h5>Email (unconfirmed)</h5>
                    <%= link_to h(user.unconfirmed_email), "mailto:#{user.unconfirmed_email}" %>
                  <% end %>

                  <h5>ORCID</h5>
                  <%= link_to h(user.orcid), user.orcid %>

                  <% if user.api_key.present? %>
                    <h5>API key</h5>
                    <%= user.api_key %>
                  <% end %>

                  <h5>Role</h5>
                  <%= user.role.capitalize %>

                  <% if user.member.present? %>
                    <h5>Member</h5>
                    <%= link_to h(user.member.title), members_path(id: user.member.name) %>
                  <% end %>

                  <h5>Account created</h5>
                  <%= l user.created_at, format: :medium %>

                  <div class="btn-toolbar pull-right">
                    <div class="btn-group btn-group-sm orcid">
                      <%= link_to 'expire token', user_path(user, user: { expires_at: Time.zone.now }), { method: :put, remote: true, class: 'btn btn-warning' } %>
                    </div>
                  </div>
                </div>
              </div>
            <% end %>
          </div>
        <% end %>
      </div>
      <div class="text-center">
        <%= will_paginate @users, :params => { :controller => users_path }, :renderer => BootstrapPagination::Rails %>
      </div>
    <% else %>
      <div class="alert alert-info">There are currently no users
        <% unless @role.nil? %>
            with role "<%= @role.capitalize %>"
        <% end %>
        <% unless params[:query].blank? %>
            with <em><%= params[:query] %></em> in the name, email or API key
        <% end %>
      </div>
    <% end %>
  </div>
  <div class='col-md-3'>
    <div class="panel facets">
      <div class="panel-body">
        <h4>Roles</h4>
        <ul>
          <% if @role %>
            <li class="active">
              <%= link_to icon('check-square-o').html_safe, users_path %>
              <%= @role.first.humanize %>
              <span class="number pull-right"><%= number_with_delimiter(@role.last) %></span>
            </li>
          <% else %>
            <% @roles.each do |role| %>
              <li>
                <%= link_to icon('square-o').html_safe, users_path(role: role.first) %>
                <%= role.first.humanize %>
                <span class="number pull-right"><%= number_with_delimiter(role.last) %></span>
              </li>
            <% end %>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</div>
